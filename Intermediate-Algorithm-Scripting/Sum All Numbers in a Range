
function sumAll(arr) {
  var small = Math.min(arr[0], arr[1]);
  var big = Math.max(arr[0], arr[1]);
  var counter = big;
  var summation = 0;
  while (counter >= small) {
    summation += counter;
    counter--;
  }
  return summation;
}

sumAll([1, 4]);
